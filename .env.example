# Database (for AEO Analyzer)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/jersey_proper?schema=public"

# Playwright (optional rendering fallback for JS-heavy pages)
PLAYWRIGHT_ENABLED=false

# Sanity Configuration (for blog)
# Get your project ID from https://sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
